[project]
name = "mdmdoc-mlxvlm-worker"
version = "0.1.0"
description = "MLX vision-language worker for the mdmdoc transcription benchmark (separate env, talks JSONL over stdin/stdout)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mlx-vlm>=0.1.0",
    "huggingface_hub>=0.25",
]

[tool.uv]
package = false
