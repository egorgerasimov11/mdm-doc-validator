"""Pure extraction layer: page engines, rendering, merging — no rules, no verdicts.

Shared by the transcription benchmark (`mdmdoc bench`) and, later, by the
`mdmdoc extract` command. Nothing in this package imports the rules engine.
"""
